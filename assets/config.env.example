# Copy to assets/config.env for local overrides (optional). CI: use --dart-define.
ONESIGNAL_APP_ID=

# Optional: scripts/download_recovery_stock.py (never commit real keys)
# PEXELS_API_KEY=
# PIXABAY_API_KEY=
