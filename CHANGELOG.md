# Changelog

All notable changes to this project are documented in this file.

The format is based on Keep a Changelog and follows Semantic Versioning (`MAJOR.MINOR.PATCH`).

## [Unreleased]

### Added
- Flutter CI workflow (`pub get`, `analyze`, `test`).
- `notify_users` Edge Function implementation with cron auth and OneSignal dispatch.
- Architecture/operations documentation and security policy.
- Additional unit tests and integration smoke test.

### Changed
- README expanded into complete setup/runbook.

