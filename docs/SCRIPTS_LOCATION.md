# Lokalizacja skryptów reel / wideo

Skrypty i `Content-videos` dla reelsów są w katalogu obok aplikacji:

**`/Users/tpltd145/Projects/SoberSteps2026-marketing/`**

(`scripts/`, `content-videos/`, `assets/` — patrz `README.md` w tym folderze.)

Kanoniczna kopia starszych narzędzi może nadal być w:

**`/Users/tpltd145/Desktop/skrypty`**

W skryptach ustaw `ROOT` na katalog marketingu lub na projekt aplikacji — zgodnie z README w `SoberSteps2026-marketing/scripts/`.

Output tymczasowy (np. render MP4): katalog roboczy skryptu lub `.cursor/output/` w projekcie Cursor (jeśli używasz workflow z repo aplikacji).
